#ifndef _RECURSIVEANDABC_H
#define _RECURSIVEANDABC_H

#include <stdio.h>
#include <stdlib.h>
#include <strings.h>

#include "stack.h"

int randomRecursiveSolve(Stack *, char);

int checkForTreeOrTent (char, int, int, char **, int);
int problemC();
int problemB(int, int);
int problemA(char *);

#endif
